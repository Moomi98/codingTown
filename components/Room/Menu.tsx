import styled from "styled-components";

const Container = styled.div`
  position: relative;
  width: 100%;
  height: 10%;
  background-color: black;
`;

const Menu = () => {
  return <Container />;
};

export default Menu;
